spec:
  name: cd
  static_sites:
    - name: potatizer
      git:
        repo_clone_url: https://github.com/Dawntraoz/potatizer.git
        branch: main
      build_command: yarn generate
      output_dir: dist
